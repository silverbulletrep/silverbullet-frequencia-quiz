var total=15;var stepParam=new URLSearchParams(location.search).get('step');var current=stepParam?Math.max(1,Math.min(total,parseInt(stepParam,10)||1)):1;var percent=Math.round((current/total)*100);var fill=document.querySelector('.progressFill');var beacon=document.querySelector('.progressBeacon');var halo=document.querySelector('.progressHalo');var spark=document.querySelector('.progressSpark');var numCur=document.querySelector('.numberCurrent');var numTot=document.querySelector('.numberTotal');if(numCur)numCur.textContent=String(current);if(numTot)numTot.textContent=String(total);if(fill)fill.style.width=percent+'%';if(beacon){beacon.style.left=percent+'%';beacon.style.width='12px';beacon.style.height='12px';}if(halo){halo.style.width=percent+'%';}var selected=new Set();var btns=document.querySelectorAll('.optionButton');var cta=document.getElementById('continueBtn');function updateCTA(){if(cta)cta.disabled=selected.size===0;}btns.forEach(function(b){b.addEventListener('click',function(){var k=b.getAttribute('data-key');if(!k)return;if(selected.has(k)){selected.delete(k);b.classList.remove('selected');}else{selected.add(k);b.classList.add('selected');}updateCTA();});});updateCTA();if(spark){spark.style.animationDuration='3.2s';}
