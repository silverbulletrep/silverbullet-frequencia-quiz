# Story 5.1: Controle de Disparos e Contrato de Dados

**Epic:** [Epic 005: Disparo Automatico de Templates WhatsApp para Recuperacao de Vendas](./epic-005-whatsapp-recovery-dispatch.md)

---

## Status

**Ready for Review**

---

## Story

**As a** operador do funil,
**I want** uma tabela de controle e um contrato de dados claro para os disparos de recuperacao WhatsApp,
**so that** o backend possa enviar templates pelo N8N sem duplicar mensagens e sem abordar leads que ja compraram.

---

## Acceptance Criteria

1. A migration cria a tabela `whatsapp_recovery_dispatches` sem alterar destrutivamente `funnel_events`, `funnel_leads` ou views existentes.
2. A tabela possui campos suficientes para auditoria: `id`, `lead_id`, `funnel_id`, `message_type`, `country`, `trigger_event_id`, `eligible_at`, `dispatched_at`, `n8n_status`, `n8n_response`, `created_at`, `updated_at`.
3. Existe constraint/indice unico que impede mais de um registro para o mesmo `(lead_id, message_type)`.
4. `message_type` aceita somente `multibanco_reminder`, `checkout_no_purchase` e `no_checkout`.
5. `n8n_status` aceita somente `pending`, `sent`, `failed` e `skipped_purchase_found`.
6. A migration inclui indices para as consultas esperadas do dispatcher: por `lead_id`, por `message_type`, por `n8n_status` e por `eligible_at`.
7. A story documenta o contrato minimo dos dados lidos em `funnel_events`, `funnel_leads` e `vw_funnel_lead_compact`.
8. A story documenta fallback de `country` quando `vw_funnel_lead_compact.country` estiver ausente.
9. A migration possui rollback manual documentado no proprio arquivo SQL ou em comentario adjacente.

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

---

## Tasks / Subtasks

- [x] Task 1 - Confirmar schema real antes da migration (AC: 1, 7, 8)
  - [x] Ler a definicao atual de `funnel_events` e `funnel_leads` no schema do worker.
  - [x] Validar no Supabase real se `vw_funnel_lead_compact.country` existe e qual tipo retorna.
  - [x] Registrar no Dev Agent Record qualquer divergencia entre schema local e banco real.

- [x] Task 2 - Criar migration de controle de disparos (AC: 1, 2, 3, 4, 5, 6, 9)
  - [x] Criar arquivo em `SILVER-BULLET-AQUISICAO-FREQUENCIA/supabase/migrations/20260430_create_whatsapp_recovery_dispatches.sql`.
  - [x] Criar tabela `public.whatsapp_recovery_dispatches`.
  - [x] Usar `gen_random_uuid()` para `id`, consistente com o schema do worker.
  - [x] Adicionar checks para `message_type` e `n8n_status`.
  - [x] Adicionar unique constraint em `(lead_id, message_type)`.
  - [x] Adicionar indices de leitura operacional.
  - [x] Incluir comentarios de rollback manual.

- [x] Task 3 - Definir contrato de dados da recuperacao (AC: 7, 8)
  - [x] Documentar de onde vem `lead_id`, `funnel_id`, `phone`, `email`, `name`, `country` e `trigger_event_id`.
  - [x] Documentar fallback de `country`: `vw_funnel_lead_compact.country`, depois `funnel_leads.metadata.country`, depois inferencia por `page_path` (`/pt/` ou `/de/`), depois `unknown`.
  - [x] Documentar que leads sem telefone valido devem ser considerados inelegiveis para WhatsApp ate existir contato.

- [ ] Task 4 - Validar idempotencia (AC: 3, 5, 6)
  - [ ] Testar insert de dois registros com mesmo `(lead_id, message_type)` e confirmar que o segundo falha ou e ignorado pela estrategia escolhida.
  - [ ] Testar status permitidos e rejeicao de status invalido.
  - [ ] Testar que a tabela aceita armazenar resposta JSON do N8N em `n8n_response`.

- [x] Task 5 - Atualizar documentacao operacional (AC: 7, 8, 9)
  - [x] Atualizar o epic ou criar nota curta anexada com o contrato final confirmado.
  - [x] Registrar que esta story nao envia N8N; ela apenas prepara controle/idempotencia.
  - [x] Registrar ClickUp sync como pendente caso a ferramenta ClickUp continue indisponivel.

---

## Dev Notes

### Previous Story Insights

Nao ha story anterior do Epic 005 implementada. O epic foi criado como Draft em `docs/stories/epic-005-whatsapp-recovery-dispatch.md` e define que Story 5.1 deve preparar banco/idempotencia antes do dispatcher backend. [Source: docs/stories/epic-005-whatsapp-recovery-dispatch.md#Stories]

### Data Models

`funnel_leads` usa chave primaria composta `(funnel_id, lead_id)` e armazena `attributes` e `metadata` como `jsonb`; estes campos podem conter contato e pais usados pelo dispatcher. [Source: ../Worker_Funil_01/db/schema.sql]

`funnel_events` possui `event_id`, `funnel_id`, `lead_id`, `event_type`, `event_timestamp`, `received_at`, `step_id`, `page_path`, `attributes`, `purchase` e `metadata`. A nova tabela deve referenciar `trigger_event_id` como `uuid`, mas nao deve criar foreign key obrigatoria se houver risco de eventos vindos de N8N antigo sem integridade completa. [Source: ../Worker_Funil_01/db/schema.sql]

`funnel_events.event_type` no schema local aceita `page_view`, `step_view`, `lead_qualified`, `desire_selected`, `step_progress`, `checkout_start`, `lead_identified` e `purchase`. O evento `PURCHASE_BILLET_PRINTED` aparece como dependencia do N8N Hotmart e deve ser validado no banco real antes de qualquer check constraint nova em tabelas existentes. [Source: ../Worker_Funil_01/db/schema.sql] [Source: hotmart-icN8N.md]

### Runtime Audit Confirmed on 2026-04-30

- Leitura read-only em `vw_funnel_lead_compact` confirmou que a coluna `country` existe no banco real e retorna valor textual nulavel. Na mesma amostra houve linhas com `country = null` e linha com `country = "PT"`.
- Leitura read-only em `funnel_events` confirmou as colunas do schema local (`event_id`, `funnel_id`, `lead_id`, `event_type`, `event_timestamp`, `received_at`, `step_id`, `page_path`, `attributes`, `purchase`, `metadata`) sem necessidade de alteracao destrutiva.
- Amostras reais de `lead_identified` em `step_id = /resultado` mostraram `attributes.phone`, `attributes.email` e `attributes.name`, mas com varios casos vazios ou malformados; por isso telefone valido continua precondicao obrigatoria antes do WhatsApp.
- Amostras reais de `checkout_start` em `step_id = /fim` vieram com `page_path = null` e `attributes = null`, entao o backend nao deve depender desse evento para derivar `country`.

### Recovery Data Contract

| Field | Primary Source | Fallback / Rule |
|---|---|---|
| `lead_id` | `funnel_events.lead_id` | Deve coincidir com `funnel_leads.lead_id` e `vw_funnel_lead_compact.lead_id` |
| `funnel_id` | `funnel_events.funnel_id` | Fallback para `funnel_leads.funnel_id` ou `vw_funnel_lead_compact.funnel_id` |
| `phone` | `funnel_events.attributes.phone` do `lead_identified` em `/resultado` | Se ausente, tentar `funnel_leads.attributes.phone` ou `funnel_leads.metadata.phone`; sem telefone valido o lead fica inelegivel |
| `email` | `funnel_events.attributes.email` do `lead_identified` em `/resultado` | Fallback para `funnel_leads.attributes.email` ou `funnel_leads.metadata.email` |
| `name` | `funnel_events.attributes.name` do `lead_identified` em `/resultado` | Fallback para `funnel_leads.attributes.name` ou `funnel_leads.metadata.name` |
| `country` | `vw_funnel_lead_compact.country` | Depois `funnel_leads.metadata.country`, depois inferencia por `page_path` (`/pt/` -> `PT`, `/de/` -> `DE`), depois `unknown` |
| `trigger_event_id` | `funnel_events.event_id` do evento que abriu a janela de recovery | Armazenado como `uuid` sem foreign key obrigatoria |

### Country Fallback Notes

- `vw_funnel_lead_compact.country` existe, mas pode vir `null`; o fallback nao e opcional.
- O frontend ja captura `country` por IP/locale e o reaproveita no tracker (`InitialQuestions.jsx`, `funnelTracker.ts`), entao `funnel_leads.metadata.country` continua sendo a melhor segunda fonte.
- `checkout_start` nao serve como fallback confiavel de pais porque os exemplos reais vieram sem `page_path`.
- Se nenhuma fonte produzir `PT` ou `DE`, persistir `unknown` na tabela e deixar Story 5.2 bloquear envio real nesses casos.

### Proposed Table Contract

Tabela: `public.whatsapp_recovery_dispatches`

Campos minimos:

| Field | Type | Notes |
|---|---|---|
| `id` | `uuid` | Primary key, default `gen_random_uuid()` |
| `lead_id` | `text` | Lead alvo do disparo |
| `funnel_id` | `text` | Funil de origem, esperado `quiz_frequencia_01` |
| `message_type` | `text` | `multibanco_reminder`, `checkout_no_purchase`, `no_checkout` |
| `country` | `text` | Pais enviado ao N8N; usar fallback documentado |
| `trigger_event_id` | `uuid null` | Evento que tornou o lead elegivel |
| `eligible_at` | `timestamptz` | Momento em que a janela de 10/25 min passou |
| `dispatched_at` | `timestamptz null` | Momento do envio real ao N8N |
| `n8n_status` | `text` | `pending`, `sent`, `failed`, `skipped_purchase_found` |
| `n8n_response` | `jsonb` | Resposta ou erro do N8N |
| `created_at` | `timestamptz` | Default `now()` |
| `updated_at` | `timestamptz` | Atualizado por trigger ou explicitamente |

Implementado em `SILVER-BULLET-AQUISICAO-FREQUENCIA/supabase/migrations/20260430_create_whatsapp_recovery_dispatches.sql`.

### API / Backend Context

O backend Express registra rotas via `app.use('/api/...', routes)` em `BACKEND/api/app.ts`. A rota do dispatcher sera Story 5.2; esta story deve apenas criar a base de dados para ela. [Source: BACKEND/api/app.ts]

O padrao atual de N8N usa Axios com timeout configuravel em `BACKEND/api/lib/n8n.ts`. A tabela desta story deve armazenar o resultado que Story 5.2 receber desse sender. [Source: BACKEND/api/lib/n8n.ts]

### N8N / Purchase Context

O fluxo documentado de purchase envia dados ao N8N somente depois de validar Stripe/PayPal no backend. A recuperacao deve respeitar esses eventos de compra como bloqueio antes de disparar WhatsApp. [Source: SILVER-BULLET-AQUISICAO-FREQUENCIA/docs/architecture/n8n_flow_representation.md#1-stripe-flow] [Source: SILVER-BULLET-AQUISICAO-FREQUENCIA/docs/architecture/n8n_flow_representation.md#2-paypal-flow]

`purchaseN8N.md` registra eventos `purchase` em `funnel_events`; `hotmart-icN8N.md` registra eventos Hotmart com `attributes.payment_type`, incluindo o dado necessario para Multibanco/CASHPAYMENT. [Source: purchaseN8N.md] [Source: hotmart-icN8N.md]

### File Locations

- Nova migration: `SILVER-BULLET-AQUISICAO-FREQUENCIA/supabase/migrations/20260430_create_whatsapp_recovery_dispatches.sql`
- Epic origem: `SILVER-BULLET-AQUISICAO-FREQUENCIA/docs/stories/epic-005-whatsapp-recovery-dispatch.md`
- Story atual: `SILVER-BULLET-AQUISICAO-FREQUENCIA/docs/stories/5.1.recovery-dispatch-control.md`
- Nota operacional desta sessao: `docs/sessions/2026-04/whatsapp-recovery-story-5-1-2026-04-30.md`

### Project Structure Notes

O `core-config.yaml` aponta `devStoryLocation: docs/stories`, mas neste workspace os stories existentes estao em `SILVER-BULLET-AQUISICAO-FREQUENCIA/docs/stories`. Esta story segue o local real dos stories ja usados no projeto. [Source: .aios-core/core-config.yaml] [Source: SILVER-BULLET-AQUISICAO-FREQUENCIA/docs/stories/3.1.story.md]

O workflow SM pede sincronizacao ClickUp, mas nenhuma ferramenta ClickUp esta disponivel nesta sessao. A story foi criada localmente e deve ser sincronizada manualmente depois, se ClickUp for obrigatorio.

### Testing

Validacao executada nesta sessao:

- Leitura read-only no Supabase REST confirmou `vw_funnel_lead_compact.country` existente e nulavel, alem de amostras reais de `lead_identified` e `checkout_start`.
- Revisao manual da migration confirmou `unique (lead_id, message_type)`, checks de `message_type`/`n8n_status`, indices operacionais e rollback manual comentado.
- Tentativa de `supabase db lint` nao concluiu porque o ambiente local nao estava com Postgres local em execucao.
- Tentativa de subir um Postgres temporario local falhou porque o workspace possui binarios cliente `libpq`, mas nao o binario `postgres` do servidor.

Validacao ainda pendente antes de marcar a story como Done:

- Rodar a migration em ambiente de desenvolvimento/staging.
- Inserir fixture com `lead_id = lead_test` e `message_type = checkout_no_purchase`.
- Tentar inserir novamente o mesmo par e confirmar bloqueio de duplicidade.
- Inserir status invalido em `n8n_status` e confirmar rejeicao.
- Inserir message type invalido e confirmar rejeicao.
- Confirmar que queries por `n8n_status = 'pending'` e `eligible_at <= now()` usam indice adequado via explain, se disponivel.

---

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-05-01 | 0.1 | Story draft inicial criada via @sm `*draft` | River (SM) |
| 2026-04-30 | 0.2 | Migration criada, contrato confirmado com audit real e handoff operacional anexado | Codex |

---

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- Audit local: `../Worker_Funil_01/db/schema.sql`
- Audit read-only no banco real: `vw_funnel_lead_compact`, `funnel_events` (`lead_identified` em `/resultado`, `checkout_start` em `/fim`)
- Tentativa de validacao local: `supabase db lint` e bootstrap de Postgres temporario em `/private/tmp`

### Completion Notes List

- Criada a migration `20260430_create_whatsapp_recovery_dispatches.sql` com tabela, trigger de `updated_at`, checks, indices e rollback manual.
- Confirmado no banco real que `vw_funnel_lead_compact.country` existe, mas pode vir `null`; o fallback documentado permanece obrigatorio.
- Confirmado com amostras reais que `lead_identified` em `/resultado` carrega contato, mas telefone pode estar vazio ou malformado.
- Confirmado com amostras reais que `checkout_start` em `/fim` pode chegar com `page_path = null`, entao nao serve como fallback principal de pais.
- Esta story nao envia N8N; ela apenas prepara persistencia, auditoria e idempotencia para a Story 5.2.
- ClickUp sync continua pendente porque a ferramenta nao esta disponivel nesta sessao.

### File List

- `SILVER-BULLET-AQUISICAO-FREQUENCIA/supabase/migrations/20260430_create_whatsapp_recovery_dispatches.sql`
- `SILVER-BULLET-AQUISICAO-FREQUENCIA/docs/stories/5.1.recovery-dispatch-control.md`
- `docs/sessions/2026-04/whatsapp-recovery-story-5-1-2026-04-30.md`

---

## QA Results

Nao executado por QA nesta sessao.

Risco residual:

- Falta rodar a migration em banco de desenvolvimento/staging para validar inserts reais de duplicidade, checks e `jsonb`.

---

## Story Draft Checklist Result

| Category | Status | Issues |
|---|---|---|
| Goal & Context Clarity | PASS | Goal, business value and relation to Epic 005 are explicit. |
| Technical Implementation Guidance | PASS | Migration path, table contract, constraints, indices and data sources are specified. |
| Reference Effectiveness | PASS | References point to source files and relevant docs used during audit. |
| Self-Containment Assessment | PASS | Core schema, fields, fallback and idempotency requirements are included directly. |
| Testing Guidance | PASS | SQL/manual validation scenarios are listed. |
| CodeRabbit Integration | N/A | Disabled in `core-config.yaml`; manual review required. |

**Final Assessment:** READY for PO/dev review, with one operational caveat: ClickUp sync was not executed because ClickUp tooling is unavailable in this session.
