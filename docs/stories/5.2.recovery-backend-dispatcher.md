# Story 5.2: Dispatcher Backend Interno

**Epic:** [Epic 005: Disparo Automatico de Templates WhatsApp para Recuperacao de Vendas](./epic-005-whatsapp-recovery-dispatch.md)

---

## Status

**Ready for Review**

---

## Story

**As a** operador do funil,
**I want** um endpoint backend que encontre leads elegiveis e envie o payload correto ao N8N,
**so that** a recuperacao WhatsApp aconteca automaticamente, com regras auditaveis e sem mensagens duplicadas.

---

## Acceptance Criteria

1. Existe rota `POST /api/recovery/dispatch-due`, registrada em `BACKEND/api/app.ts` via `app.use('/api/recovery', recoveryRoutes)`.
2. A rota aceita `dry_run`, `limit` e filtro opcional por `lead_id`.
3. A rota exige protecao operacional, preferencialmente `RECOVERY_DISPATCH_SECRET`, antes de executar qualquer consulta ou envio.
4. `dry_run = true` retorna candidatos e motivos de elegibilidade sem inserir dispatch e sem enviar payload ao N8N.
5. Execucao real cria/usa registro em `whatsapp_recovery_dispatches` e respeita unique `(lead_id, message_type)`.
6. Regra `multibanco_reminder`: tem `PURCHASE_BILLET_PRINTED`, `attributes.payment_type = CASHPAYMENT`, passou 10 minutos e nao tem `purchase`.
7. Regra `checkout_no_purchase`: tem `checkout_start`, nao tem `purchase`, passou 10 minutos e nao e `CASHPAYMENT`.
8. Regra `no_checkout`: nao tem `checkout_start`, nao tem `purchase`, passou 25 minutos desde `lead_identified` em `/resultado`; se nao existir, usar primeiro evento do lead no funil.
9. Se um lead encaixar em mais de uma regra, a prioridade e `multibanco_reminder`, depois `checkout_no_purchase`, depois `no_checkout`.
10. Antes de enviar N8N, o backend faz uma checagem final de `purchase`; se encontrar compra, marca/retorna `skipped_purchase_found`.
11. Payload enviado ao N8N contem `lead_id`, `message_type`, `country`, `language`, `phone`, `email`, `name`, `funnel_id` e `trigger`.
12. Falha do N8N fica registrada em `n8n_status = failed` com resposta/erro em `n8n_response`, sem marcar `dispatched_at`.
13. Sucesso do N8N fica registrado em `n8n_status = sent` com `dispatched_at`.
14. Build TypeScript do backend passa com `npm run build:api`.

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in `core-config.yaml`

---

## Tasks / Subtasks

- [x] Task 1 - Criar rota protegida de recovery (AC: 1, 2, 3)
  - [x] Criar `BACKEND/api/routes/recovery.ts`.
  - [x] Registrar a rota em `BACKEND/api/app.ts`.
  - [x] Validar `RECOVERY_DISPATCH_SECRET` via header, por exemplo `x-recovery-dispatch-secret`.
  - [x] Rejeitar requests sem segredo com `401` ou `403`.

- [x] Task 2 - Criar tipos e contrato do dispatcher (AC: 2, 4, 11)
  - [x] Criar `BACKEND/api/lib/recoveryTypes.ts`.
  - [x] Definir `RecoveryMessageType`, `RecoveryCandidate`, `RecoveryDispatchResult` e `RecoveryN8NPayload`.
  - [x] Normalizar `limit` com maximo seguro, recomendado `100`.
  - [x] Definir resposta de `dry_run` com `candidate_count`, `candidates` e `skipped`.

- [x] Task 3 - Criar sender N8N de templates (AC: 11, 12, 13)
  - [x] Criar `BACKEND/api/lib/recoveryN8N.ts`.
  - [x] Usar `N8N_META_TEMPLATE_WEBHOOK_URL`.
  - [x] Reusar padrao de Axios, headers JSON, timeout e logs de `BACKEND/api/lib/n8n.ts`.
  - [x] Nao logar secrets, tokens ou headers sensiveis.

- [x] Task 4 - Implementar elegibilidade (AC: 4, 6, 7, 8, 9, 10)
  - [x] Criar `BACKEND/api/lib/recoveryDispatcher.ts`.
  - [x] Buscar eventos candidatos em `funnel_events` e contexto em `funnel_leads`/`vw_funnel_lead_compact`.
  - [x] Implementar `multibanco_reminder`.
  - [x] Implementar `checkout_no_purchase`.
  - [x] Implementar `no_checkout`.
  - [x] Aplicar prioridade de mensagem por lead.
  - [x] Filtrar leads sem telefone valido.
  - [x] Fazer checagem final de compra imediatamente antes de enviar.

- [x] Task 5 - Implementar idempotencia e persistencia (AC: 5, 10, 12, 13)
  - [x] Inserir registro em `whatsapp_recovery_dispatches` antes do envio real.
  - [x] Se o insert conflitar com `(lead_id, message_type)`, retornar como `already_dispatched` ou equivalente.
  - [x] Atualizar status para `sent`, `failed` ou `skipped_purchase_found`.
  - [x] Garantir que reexecutar o endpoint nao reenvia mensagem ja enviada.

- [ ] Task 6 - Testes e verificacao (AC: 4, 5, 6, 7, 8, 9, 10, 14)
  - [x] Adicionar testes unitarios para as 3 regras com fixtures pequenas.
  - [x] Testar prioridade quando um lead tem eventos sobrepostos.
  - [ ] Testar `dry_run` sem insert e sem chamada N8N.
  - [x] Testar compra encontrada antes do envio.
  - [x] Rodar `npm run build:api` em `BACKEND`.

---

## Dev Notes

### Previous Story Insights

Story 5.1 define a tabela `whatsapp_recovery_dispatches`, os status permitidos e a idempotencia por `(lead_id, message_type)`. Esta story depende dessa migration ou de tabela equivalente aplicada em ambiente de desenvolvimento. [Source: docs/stories/5.1.recovery-dispatch-control.md#Proposed-Table-Contract]

### Backend Patterns

O backend Express carrega env com `dotenv.config()`, usa `express.json({ limit: '10mb' })` e registra rotas por prefixo em `BACKEND/api/app.ts`. A nova rota deve seguir o mesmo padrao de `authRoutes`, `stripeRoutes`, `paypalRoutes` e `leadsRoutes`. [Source: BACKEND/api/app.ts]

O servidor sobe em `BACKEND/api/server.ts` e trata `SIGTERM`/`SIGINT`. Esta story deve criar apenas rota/service; ativacao por intervalo fica na Story 5.3. [Source: BACKEND/api/server.ts]

`BACKEND/api/lib/n8n.ts` usa Axios, `N8N_TIMEOUT_MS`, header `Content-Type: application/json`, logs de sucesso/erro e retorno booleano. O sender de recovery deve reaproveitar esse estilo, mas usar env propria `N8N_META_TEMPLATE_WEBHOOK_URL`. [Source: BACKEND/api/lib/n8n.ts]

### Existing Purchase Blocking

Stripe e PayPal ja enviam payload enriquecido para N8N apos confirmacao de compra. O dispatcher de recovery deve tratar qualquer evento `purchase` como bloqueio definitivo antes do envio. [Source: BACKEND/api/routes/stripe.ts] [Source: BACKEND/api/routes/paypal.ts]

O fluxo N8N de purchase registra compra em `funnel_events`. Isso permite o dispatcher consultar o banco como fonte da verdade para cancelar recuperacao. [Source: purchaseN8N.md]

### Event Data Contract

O worker grava eventos em `funnel_events` com `event_id`, `funnel_id`, `lead_id`, `event_type`, `event_timestamp`, `step_id`, `page_path`, `attributes`, `purchase` e `metadata`. [Source: ../Worker_Funil_01/db/schema.sql]

O frontend gera `checkout_start` e `lead_identified` via `funnelTracker.ts`; `lead_identified` inclui `attributes: { name, email, phone }`. [Source: SILVER-BULLET-AQUISICAO-FREQUENCIA/src/lib/funnelTracker.ts]

O N8N Hotmart registra `attributes.payment_type` com o tipo de pagamento. Essa informacao e essencial para separar `CASHPAYMENT`/Multibanco de checkout comum. [Source: hotmart-icN8N.md]

Leitura read-only em `vw_funnel_lead_compact` em 2026-04-30 confirmou que a view real ja expõe `funnel_id`, `lead_id`, `name`, `email`, `phone`, `country`, `has_purchase`, `last_event_at` e `auto_tag`; a implementação desta story usa esses campos como contexto preferencial e faz fallback para `funnel_events`/`funnel_leads` quando necessário.

### Candidate Rules

`multibanco_reminder`:

```text
event_type = PURCHASE_BILLET_PRINTED
attributes.payment_type = CASHPAYMENT
event_timestamp <= now() - 10 minutes
no purchase exists for lead
```

`checkout_no_purchase`:

```text
event_type = checkout_start
event_timestamp <= now() - 10 minutes
no purchase exists for lead
no CASHPAYMENT/PURCHASE_BILLET_PRINTED condition for same lead
```

`no_checkout`:

```text
no checkout_start exists for lead
no purchase exists for lead
base event = first lead_identified at step_id /resultado
fallback base event = first event for lead in funnel
base event timestamp <= now() - 25 minutes
```

### Payload Contract

Payload minimo para `N8N_META_TEMPLATE_WEBHOOK_URL`:

```json
{
  "lead_id": "lead_x",
  "message_type": "checkout_no_purchase",
  "country": "PT",
  "language": "pt",
  "phone": "351...",
  "email": "lead@email.com",
  "name": "Nome",
  "funnel_id": "quiz_frequencia_01",
  "trigger": {
    "event_id": "uuid",
    "event_type": "checkout_start",
    "event_timestamp": "2026-05-01T12:00:00.000Z",
    "payment_type": null
  }
}
```

`language` deve ser derivado de `country` ou `page_path`: `PT` -> `pt`, `DE` -> `de`, fallback `pt` somente se o negocio confirmar. Se incerto, retornar `unknown` no `dry_run` e nao enviar em modo real ate corrigir.

### File Locations

- Nova rota: `BACKEND/api/routes/recovery.ts`
- Novo service: `BACKEND/api/lib/recoveryDispatcher.ts`
- Tipos: `BACKEND/api/lib/recoveryTypes.ts`
- Sender N8N: `BACKEND/api/lib/recoveryN8N.ts`
- Supabase admin helper: `BACKEND/api/lib/supabaseAdmin.ts`
- Testes: `BACKEND/api/lib/__tests__/recoveryDispatcher.test.ts` ou caminho equivalente definido pelo dev

### Project Structure Notes

O `BACKEND/package.json` recebeu o script `test:api`, baseado em `node:test` sobre o artefato compilado em `dist/`, para evitar adicionar framework novo ao backend. Build existente: `npm run build:api`. [Source: BACKEND/package.json]

ClickUp sync nao foi executado nesta sessao porque a ferramenta ClickUp nao esta disponivel; se obrigatorio, sincronizar manualmente depois.

### Testing

Validacao executada nesta sessao:

- `npm ci` em `BACKEND` para instalar dependencias locais do backend.
- `npm run build:api` em `BACKEND`: passou.
- `npm run test:api` em `BACKEND`: passou com 5/5 testes.
- Testes automatizados cobrem `multibanco_reminder`, `no_checkout`, bloqueio por `purchase`, prioridade sobreposta e filtro de telefone invalido.

Validacao ainda pendente antes de marcar como Done:

- Teste manual com `POST /api/recovery/dispatch-due`, `dry_run=true`, segredo valido e `lead_id` especifico.
- Validacao manual de insert/update real em `whatsapp_recovery_dispatches` contra um ambiente com `SUPABASE_SERVICE_ROLE_KEY`.
- Verificar regressao final com Stripe/PayPal em ambiente integrado, embora o backend compile com as rotas existentes.

---

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-05-01 | 0.1 | Story draft inicial criada via @sm `*draft` | River (SM) |
| 2026-04-30 | 0.2 | Dispatcher backend implementado com rota protegida, sender N8N, testes unitarios e build validado | Codex |

---

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- Build: `npm run build:api` em `BACKEND`
- Testes: `npm run test:api` em `BACKEND`
- Audit read-only da view real: `vw_funnel_lead_compact?select=*&limit=1`

### Completion Notes List

- Criada a rota `POST /api/recovery/dispatch-due` com validacao de `x-recovery-dispatch-secret`.
- Implementado dispatcher com busca em `vw_funnel_lead_compact`, `funnel_leads` e `funnel_events`.
- Implementadas as 3 regras de elegibilidade com prioridade por lead e bloqueio por `purchase`.
- Implementado sender dedicado `N8N_META_TEMPLATE_WEBHOOK_URL`, sem logar segredo/headers sensiveis.
- Implementada persistencia em `whatsapp_recovery_dispatches` com tratamento de conflito como `already_dispatched`.
- Envio real bloqueia `language = unknown`; `dry_run` continua expondo o candidato para diagnostico.
- Testes unitarios e build do backend passaram localmente apos `npm ci`.
- Teste manual com segredo/Supabase real continua pendente nesta sessao.

### File List

- `BACKEND/api/app.ts`
- `BACKEND/api/routes/recovery.ts`
- `BACKEND/api/lib/recoveryDispatcher.ts`
- `BACKEND/api/lib/recoveryN8N.ts`
- `BACKEND/api/lib/recoveryTypes.ts`
- `BACKEND/api/lib/supabaseAdmin.ts`
- `BACKEND/api/lib/__tests__/recoveryDispatcher.test.ts`
- `BACKEND/package.json`
- `SILVER-BULLET-AQUISICAO-FREQUENCIA/docs/stories/5.2.recovery-backend-dispatcher.md`
- `docs/sessions/2026-04/whatsapp-recovery-story-5-2-2026-04-30.md`

---

## QA Results

Nao executado por QA nesta sessao.

Risco residual:

- Falta executar a rota contra credenciais reais de backend para validar `dry_run` e escrita real em `whatsapp_recovery_dispatches`.

---

## Story Draft Checklist Result

| Category | Status | Issues |
|---|---|---|
| Goal & Context Clarity | PASS | Story explains endpoint, business value and dependency on Story 5.1. |
| Technical Implementation Guidance | PASS | Route, services, payload, env var, rules and persistence flow are specified. |
| Reference Effectiveness | PASS | References point to backend, worker schema, N8N docs and previous story. |
| Self-Containment Assessment | PASS | Rules and payload are included directly. |
| Testing Guidance | PASS | Unit, dry-run, build and regression checks are listed. |
| CodeRabbit Integration | N/A | Disabled in `core-config.yaml`; manual review required. |

**Final Assessment:** READY for PO/dev review, assuming Story 5.1 migration is applied before implementation.
